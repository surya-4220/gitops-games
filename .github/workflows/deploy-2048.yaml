# ============================================================
# .github/workflows/deploy.yml
# CI/CD Pipeline — Creates ArgoCD Applications on push
# ============================================================

name: Deploy Games via ArgoCD

on:
  push:
    branches:
      - main
    paths:
      - '2048/**'
      - 'apps/argocd-2048.yaml'
jobs:
  deploy-2048-game:
    name: Deploy 2048 Game
    runs-on: ubuntu-latest
    # Only run if 2048game manifests or argocd app definition changed

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ap-south-1

    - name: Update kubeconfig
      run: |
        aws eks update-kubeconfig \
          --region ap-south-1 \
          --name agrocd

    - name: Create ArgoCD Application for 2048 Game
      run: |
        kubectl apply -f apps/argocd-2048.yaml

    - name: Wait for 2048 Game to sync
      run: |
        echo "Waiting for ArgoCD to sync 2048-game..."
        timeout 120 bash -c '
          until kubectl get application 2048-game -n argocd \
            -o jsonpath="{.status.sync.status}" 2>/dev/null | grep -q "Synced"; do
            echo "Still syncing..."
            sleep 10
          done
        '
        echo "✅ 2048 Game synced successfully!"

    - name: Check 2048 Game health
      run: |
        timeout 120 bash -c '
          until kubectl get application 2048-game -n argocd \
            -o jsonpath="{.status.health.status}" 2>/dev/null | grep -q "Healthy"; do
            echo "Waiting for healthy status..."
            sleep 10
          done
        '
        echo "✅ 2048 Game is Healthy!"

    - name: Print 2048 Game URL
      run: |
        echo "Waiting for LoadBalancer IP..."
        sleep 30
        kubectl get svc -n 2048-game
        echo ""
        echo "🎮 2048 Game URL:"
        kubectl get svc 2048-game-svc -n 2048-game \
          -o jsonpath="{.status.loadBalancer.ingress[0].hostname}" && echo

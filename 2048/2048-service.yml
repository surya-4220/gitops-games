apiVersion: v1
kind: Service
metadata:
  name: 2048-game-svc
  namespace: 2048-game
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  type: LoadBalancer
  selector:
    app: 2048-game
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
